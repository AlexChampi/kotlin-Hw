spring:
  application.name: HomeWork3
  profiles.active: test
  liquibase:
    change-log: /db/changelog/db.changelog.xml
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: Aleksandr
    password: 12345
server.port: 9000

room.address: http://localhost:9000
